<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Усі сни</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1>Список снів</h1>

    <% if (dreams.length === 0) { %>
    <p>Поки що немає жодного сну.</p>
    <% } else { %>
    <ul>
      <% dreams.forEach(dream => { %>
      <li>
        <h2><%= dream.title %></h2>
        <p><strong>Опис:</strong> <%= dream.description %></p>
        <p><strong>Категорії:</strong> <%= dream.categories.join(', ') %></p>
        <p><strong>Дата:</strong> <%= dream.date ? dream.date.toDateString() : "Невідома" %></p>
      </li>
      <% }) %>
    </ul>
    <% } %>

    <a href="/add_new_dreams">Додати новий сон</a>
  </body>
</html>
