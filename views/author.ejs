<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Author</title>
    <%- include("header") %>
  </head>
  <body>
    <h1>Author Profile</h1>
    <p><strong>Username:</strong> <%= author.username %></p>
    <p><strong>Email:</strong> <%= author.email %></p>

    <h2>Dreams by <%= author.username %></h2>
    <ul>
      <% if (dreams.length > 0) { %> <% dreams.forEach(dream => { %>
      <li>
        <a href="/dream/<%= dream._id %>"><%= dream.title %></a>
      </li>

      <p><strong>Reactions:</strong></p>
      <p>
        🔥 <%= dream.reactions.very_cool %> 😊 <%= dream.reactions.pleasant %> 😂 <%=
        dream.reactions.funny %> 🤯 <%= dream.reactions.mind_blown %> 😳 <%= dream.reactions.weird
        %> 👎 <%= dream.reactions.dislike %>
      </p>
      <hr />
      <% }) %> <% } else { %>
      <p>This author hasn't posted any dreams yet.</p>
      <% } %>
    </ul>
  </body>
</html>
